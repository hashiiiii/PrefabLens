---
title: PrefabLens | CLI demo
permalink: cli.html
layout: layout.liquid
---
<h1>CLI demo</h1>
<p class="hint">This page shows actual output from the <code>prefablens</code> binary. The site build creates this output.</p>

<h2 id="terminal"><a href="#terminal">Terminal</a></h2>
<p><code>prefablens main</code> compares a ref with your working tree.</p>
<div class="window terminal">
  <div class="window-title">default</div>
  <pre>{% rawHtml "terminal.html" %}</pre>
</div>

<h2 id="html-report"><a href="#html-report">HTML report</a></h2>
<p>
  <code>prefablens --open main</code> writes a self-contained report.
  <a href="cli-report.html" target="_blank" rel="noopener">Open the report as a full page</a>.
</p>
<div class="window">
  <div class="window-title">html</div>
  <iframe class="report-frame" src="cli-report.html" title="PrefabLens HTML report"></iframe>
</div>

<h2 id="git-merge"><a href="#git-merge">Git merge</a></h2>
<p>
  After <code>prefablens setup-merge</code>, <code>git merge</code> opens the PrefabLens TUI for UnityYAML
  conflicts.
</p>
<div class="window">
  <div class="window-title">git merge</div>
  <video
    class="window-shot"
    src="images/cli-git-merge.mp4"
    width="960"
    height="626"
    controls
    playsinline
    preload="metadata"
  >
    PrefabLens resolving a UnityYAML merge conflict in the terminal.
  </video>
</div>

<h2 id="gui-git-clients"><a href="#gui-git-clients">GUI Git clients</a></h2>
<p>
  For GUI Git clients on macOS and Windows, set the installed PrefabLens executable as the external merge tool.
  Use these arguments:
</p>
<pre><code>mergetool --terminal &lt;base&gt; &lt;local&gt; &lt;remote&gt; &lt;merged&gt;</code></pre>
<p>
  Replace the four paths with your client's placeholders.
  Open a conflicted file with the external merge tool.
  Resolve it in PrefabLens, then return to your Git client to stage it.
</p>
<div class="window">
  <div class="window-title"><a href="https://fork.dev/">Fork</a> example</div>
  <video
    class="window-shot"
    src="images/cli-fork-merge.mp4"
    width="1750"
    height="982"
    aria-label="Fork merge example"
    controls
    playsinline
    preload="metadata"
  >
    PrefabLens resolving a UnityYAML merge conflict from Fork.
  </video>
</div>

<details>
  <summary>Windows: Could not find merge strategy 'prefablens'</summary>
  <p>
    After installing PrefabLens, fully close and reopen your Git client to load the updated <code>Path</code>.
    A running client may still have a <code>Path</code> that predates the installation.
    Check discovery in PowerShell:
  </p>
  <pre><code>prefablens --version
git merge-prefablens --version
git --list-cmds=others | Select-String '^merge-prefablens$'</code></pre>
  <p>
    The versions should match, and the last command should print <code>merge-prefablens</code>.
    If discovery fails, add the folder containing both <code>prefablens.exe</code> and
    <code>git-merge-prefablens</code> to your user <code>Path</code>.
    For Scoop, <code>scoop prefix prefablens</code> shows this folder.
    Open a new PowerShell window and check again.
  </p>
  <p>
    If PowerShell finds the strategy but restarting your Git client does not help, launch it from that PowerShell window.
    For Fork, first save its executable path while it is running:
  </p>
  <pre><code>$forkExe = (Get-Process -Name Fork | Select-Object -First 1).Path</code></pre>
  <p>Fully exit Fork, then run this in the same PowerShell window:</p>
  <pre><code>Start-Process -FilePath $forkExe</code></pre>
  <p>
    Fork inherits that window's <code>Path</code>.
    Keep your merge setup and retry the merge in Fork.
    If it still fails, check the Git executable selected in Fork's preferences.
  </p>
</details>
