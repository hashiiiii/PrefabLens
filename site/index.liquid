---
title: PrefabLens
permalink: index.html
layout: layout.liquid
---
<h1>Human-readable diffs for UnityYAML assets</h1>
<p>
  PrefabLens shows UnityYAML diffs in a view like the Hierarchy and the Inspector.
  You can use PrefabLens on GitHub pull requests, in the terminal, and in the Unity Editor.
</p>

<!--
  Same Robot.prefab change, both views from the real tools at build time:
  git for the raw diff, the prefablens binary for the report.
-->
<div class="compare">
  <div>
    <p class="label">Without PrefabLens</p>
    <div class="window">
      <div class="window-title">Robot.prefab</div>
      <div class="gh-page raw-pane">{% rawHtml "hero-diff.html" %}</div>
    </div>
  </div>
  <div>
    <p class="label">With PrefabLens</p>
    <div class="window">
      <div class="window-title">Robot.prefab</div>
      <iframe class="hero-frame" src="hero-report.html" title="PrefabLens human-readable diff"></iframe>
    </div>
  </div>
</div>

<h2>Demos</h2>
<ul>
  <li>
    <a href="extension.html">GitHub pull requests</a>: a simulated Files changed page with the real
    extension and the WASM diff engine.
  </li>
  <li><a href="cli.html">CLI</a>: the terminal tree and the HTML report.</li>
  <li><a href="editor.html">Unity Editor</a>: a window that shows a diff for every changed asset.</li>
</ul>

<h2>Install</h2>

<h3>Chrome extension</h3>
<p><a href="https://chromewebstore.google.com/detail/dlhnalbfkikchkfedfneiimadommcnip">Chrome Web Store</a></p>

<h3>CLI</h3>
<pre><code><span class="cmd">brew</span> install hashiiiii/tap/prefablens</code></pre>
<pre><code><span class="cmd">scoop</span> bucket add hashiiiii https://github.com/hashiiiii/scoop-bucket
<span class="cmd">scoop</span> install prefablens</code></pre>
<pre><code><span class="cmd">mise</span> use -g github:hashiiiii/PrefabLens</code></pre>

<h3>Unity Editor</h3>
<pre><code><span class="cmd">openupm</span> add com.hashiiiii.prefablens</code></pre>
<p>Or use the Package Manager git URL: <code>https://github.com/hashiiiii/PrefabLens.git?path=editor</code></p>

<h3>Other installs</h3>
<p>Get the prebuilt binaries from the <a href="https://github.com/hashiiiii/PrefabLens/releases">releases page</a>.</p>
