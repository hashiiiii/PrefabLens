{
  "name": "prefablens-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test src/*.test.mjs"
  },
  "packageManager": "pnpm@11.10.0",
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
